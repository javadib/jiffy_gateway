services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: web
    user: root
    command: uv run python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
      - ./data:/app/data
    ports:
      - "${WEB_PORT:-8080}:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS}
      REDIS_URL: ${REDIS_URL}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND}
      X_JIFFY_TOKEN: ${X_JIFFY_TOKEN}
      GITHUB_INGEST_TOKEN: ${GITHUB_INGEST_TOKEN}
      GITLAB_INGEST_TOKEN: ${GITLAB_INGEST_TOKEN}
      GITEA_INGEST_TOKEN: ${GITEA_INGEST_TOKEN}
      SANDBOX_OPENCODE_CONFIG_PATH: ${SANDBOX_OPENCODE_CONFIG_PATH}
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - jiffy-internal
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2g
        reservations:
          cpus: "0.5"
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  celery:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: celery
    command: uv run celery -A config worker -Q execute --concurrency=3 -l info -n execute@%h
    user: root
    volumes:
      - .:/app
      - ./opencode.json:/app/opencode.json
    environment:
      DOCKER_HOST: "tcp://docker-socket-proxy:2375"
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS}
      REDIS_URL: ${REDIS_URL}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND}
      X_JIFFY_TOKEN: ${X_JIFFY_TOKEN}
      GITHUB_INGEST_TOKEN: ${GITHUB_INGEST_TOKEN}
      GITLAB_INGEST_TOKEN: ${GITLAB_INGEST_TOKEN}
      GITEA_INGEST_TOKEN: ${GITEA_INGEST_TOKEN}
      SANDBOX_OPENCODE_CONFIG_PATH: ${SANDBOX_OPENCODE_CONFIG_PATH}
    depends_on:
      redis:
        condition: service_healthy
      docker-socket-proxy:
        condition: service_started
    networks:
      - jiffy-internal
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2g
        reservations:
          cpus: "0.5"
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------------------------------------------------------------------------
  # Docker Socket Proxy
  # ---------------------------------------------------------------------------
  # Proxies a restricted subset of the Docker Engine API so the Celery worker
  # can manage sandbox containers without having raw host-Docker-root access.
  #
  # Enabled endpoint groups (and why):
  #   CONTAINERS — container create/start/stop/remove (sandbox lifecycle)
  #   IMAGES     — image inspect/get (check if sandbox image exists)
  #   BUILD      — image build from Dockerfile (build sandbox image if missing)
  #   NETWORKS   — network inspect/create (jiffy-sandbox-net for sandbox containers)
  #   EXEC       — exec into running containers (clone repo, run agent)
  #   POST       — global toggle for write operations (required by all of the above)
  #
  # Everything else is disabled (defaults to 0).  The proxy is NOT exposed on
  # any host port — it is only reachable from services on jiffy-internal.
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:latest
    container_name: docker-socket-proxy
    environment:
      # Container lifecycle (create, start, stop, remove, inspect, list)
      CONTAINERS: 1
      # Image inspect/get (check sandbox image existence)
      IMAGES: 1
      # Build images from Dockerfile (sandbox image build)
      BUILD: 1
      # Network inspect/create (jiffy-sandbox-net)
      NETWORKS: 1
      # Exec into running containers (clone, agent run)
      EXEC: 1
      # Global POST toggle — required for all write operations above
      POST: 1
      # --- Everything below is intentionally disabled ---
      VOLUMES: 0
      SERVICES: 0
      TASKS: 0
      SYSTEM: 0
      PLUGINS: 0
      SECRETS: 0
      CONFIGS: 0
      NODES: 0
      SWARM: 0
    volumes:
      # Only the proxy mounts the real host socket, read-only
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - jiffy-internal
    # No ports exposed to the host — internal network only
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512m
        reservations:
          cpus: "0.25"
          memory: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  redis:
    image: redis:8.8.0
    container_name: redis
    restart: always
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
      --save 900 1
      --save 300 10
      --save 60 10000
      --tcp-keepalive 60
    volumes:
      - redis_data:/data
    networks:
      - jiffy-internal
    healthcheck:
      test: [ "CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  sandbox:
    build:
      context: ./docker/sandbox
      dockerfile: Dockerfile
    image: jiffy-sandbox:dev
    container_name: jiffy-sandbox-dev
    user: root
    stdin_open: true
    tty: true
    command: ["bash", "-c", "bash /smoke-test.sh; echo '=== Smoke test complete, container staying alive for debugging ==='; while true; do sleep 86400; done"]
    volumes:
      - .:/workspace:ro
      - ./docker/sandbox/smoke-test.sh:/smoke-test.sh:ro
    networks:
      - jiffy-internal
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1g
        reservations:
          cpus: "0.25"
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  redis_data:

networks:
  jiffy-internal:
    driver: bridge
    internal: false  # containers need outbound access for git clone, pip, npm, etc.
